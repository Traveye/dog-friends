import React from "react";
import DogImg from "../DogComponents/DogImg"

export default function DogHero(media) {
   console.log(media)
   // const style = { backgroundImage: `url(${banner})`}
   return (
      <div>
         <div><div><DogImg /></div></div>
      </div>
   );
}

